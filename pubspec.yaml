name: recipe_application
description: "Flutter project for recipes"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^14.6.2
  http: ^1.2.1
  shared_preferences: ^2.2.2
  mockito: ^5.4.4
  build_runner: ^2.4.9
  firebase_core: ^3.8.1
  cloud_firestore: ^5.5.1
  hooks_riverpod: ^2.6.1
  flutter_hooks: ^0.20.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
